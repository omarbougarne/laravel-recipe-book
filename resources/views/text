{{-- @foreach($data as $data)
                <div class="recipe">
                <h2>{{ $data->title }}</h2>
                <p>{{ $data->description }}</p>
                <img class="hidden w-48 mr-6 md:block" src="img/{{$data->image}}" alt="idk">
                </div>
                @endforeach
                @if($search_param)
                    <p>Showing results for: {{ $search_param }}</p>
                @endif
            </div> --}}